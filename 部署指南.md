# 网站部署指南

## 1. 开发时导出编译文件

### 方法一：使用部署配置文件
```bash
bundle exec jekyll build --config _config_deploy.yml
```

### 方法二：直接指定输出目录
```bash
bundle exec jekyll build --destination ./dist
```

编译完成后，所有静态文件将输出到 `dist` 目录。

## 2. 使用编译好的文件

### 本地预览
```bash
cd dist
python -m http.server 8000
```
然后访问 `http://localhost:8000`

### 部署到服务器
1. 将整个 `dist` 文件夹上传到服务器
2. 配置 Web 服务器（如 Nginx、Apache）指向 `dist` 目录
3. 确保服务器支持静态文件服务

### 部署到 GitHub Pages
1. 将 `dist` 目录内容推送到 `gh-pages` 分支
2. 或直接将 `dist` 目录内容复制到主分支根目录

## 注意事项
- `dist` 目录包含所有必需的静态文件
- 相对路径已优化，支持在任何路径下部署
- 无需 Jekyll 环境即可运行编译后的网站
